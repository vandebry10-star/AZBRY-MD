- name: Move extracted/AZBRY-MD content to repo root
        run: |
          if [ -d "extracted/AZBRY-MD" ]; then
            echo "📦 Memindahkan isi extracted/AZBRY-MD ke root repo..."
            mv extracted/AZBRY-MD/* .
            rm -rf extracted/AZBRY-MD
            echo "✅ Selesai dipindahkan."
          else
            echo "⚠️ Folder extracted/AZBRY-MD tidak ditemukan."
          fi

      - name: Commit moved files
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "github-actions[bot]"
          git add .
          git commit -m "chore: move extracted/AZBRY-MD contents to root" || echo "⚙️ Tidak ada perubahan untuk dikomit."
          git push
